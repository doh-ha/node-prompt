{
  "name": "honors-root",
  "private": true,
  "scripts": {
    "server": "uvicorn server.app.main:app --host 0.0.0.0 --port 3001 --reload",
    "web": "npm --prefix web run start",
    "dev": "concurrently \"npm run server\" \"npm run web\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
